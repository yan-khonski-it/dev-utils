class DirectoryWithRepositories:
  """Represents a directory with repositories in it."""

  def __init__(self, repositories_directory, urls):
    self.repositories_directory = repositories_directory
    self.urls = urls
